# タスク仕様書テンプレート (Task Specification)

> **対象エージェント**: Claude Code / Codex
> **目的**: 本仕様書に従い、第1ループ（抽出・アルゴリズム・テスト）の実装またはリファクタリングを自律実行してください。

---

## 1. タスク概要
- **タスクID**: `TASK-XXX`
- **対象図幅 / 対象モジュール**: 例: `scripts/age_resolution.py` または 図幅 `towada (1050)`
- **主要な変更目的**: 

---

## 2. 満たすべき不変条件 (Invariants)
1. **単調性の維持**: `b_age >= t_age >= 0.0`
2. **証拠の維持**: 年代抽出時は必ず元の日本語テキスト引用 (`verbatim_quote`) を付与すること。
3. **語彙準拠**: `config/official_vocab.json` にない勝手な造語・英語表現を出力しないこと。

---

## 3. 実行手順
1. 指定されたファイルの既存実装を確認する。
2. 変更・修正を適用する。
3. `pytest` または `python run.py check <map_id>` を実行し、全テストが通過することを確認する。
4. 変更理由とテスト結果を簡潔にまとめて完了報告を行うこと。
